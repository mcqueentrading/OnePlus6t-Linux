-- Community Edition stock-style Hyprland companion config.
-- This file is intentionally generic and is not device tuned.

return {
  mainMod = "SUPER",
  terminal = "foot",
  monitor = {
    name = "",
    mode = "preferred",
    position = "auto",
    scale = "auto",
  },
  policy = {
    deviceSpecificShell = false,
    customRotation = false,
    customGestures = false,
    customLockScreen = false,
  },
}
